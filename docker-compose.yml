services:
   the-fire-bot:
     image: ghcr.io/riskymh/the-fire-bot:latest
     build: .
     restart: unless-stopped
     init: true
     extra_hosts:
       - "host.docker.internal:host-gateway"
     environment:
       DATABASE_URL: ${DATABASE_URL:-sqlite:///data/db.sqlite}
       DISCORD_TOKEN: ${DISCORD_TOKEN:?REQUIRED}
       NODE_ENV: production
       FORCE_COLOR: 1
       BUN_JSC_forceRAMSize: 52428800 # 50MB
     volumes:
        - data:/data
     deploy:
       resources:
         limits:
           memory: 128M
         reservations:
           memory: 32M

volumes:
  data:
